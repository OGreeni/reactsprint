import React from 'react';

import LottieWrapper from '@components/lottie-wrapper';

export default function Navigation() {
  return (
    <nav className="fixed top-0 left-0 flex w-full justify-between bg-white/10 p-5 text-3xl backdrop-blur"></nav>
  );
}
